server:
  port: 8001

spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          uri: https://rep.360taihe.com/scm-group/scm-cloud.git
          search-paths: /scm-config-repo/*
          username: hzcyn
          password: hzcyn233
      label: master
  #rabbitmq:
  #   host: 10.200.131.93
  #    port: 5672
  #    username: scm
  #    password: scmDEV999

eureka:
  client:
    serviceUrl:
      defaultZone: http://registry:123456@localhost:8000/eureka/

security:
  basic:
    enabled: true
  user:
    name: admin
    password: admin

#management:
#  security:
#    enabled: false #/bus/refresh刷新git配置的时候不校验授权
  #endpoints:
  #  web:
  #    exposure:
  #      include: "*" #暴露/actuator/bus-refresh请求路径
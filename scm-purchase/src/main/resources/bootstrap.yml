spring:
  application:
    name: purchase
  cloud:
    config:
      fail-fast: true
      name: purchase
      profile: ${spring.profiles.active}
      label: master
      discovery:
        service-id: config
        enabled: true

server:
  port: 9000

eureka:
  client:
    serviceUrl:
      defaultZone: http://registry:123456@registry:8000/eureka/
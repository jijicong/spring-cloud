spring:
  application:
    name: common
  cloud:
    config:
      fail-fast: true
      name: common
      profile: ${spring.profiles.active}
      label: master
      discovery:
        service-id: config
        enabled: true
      username: admin
      password: admin

server:
  port: 9001

eureka:
  client:
    serviceUrl:
      defaultZone: http://registry:123456@localhost:8000/eureka/
